# Agent TestLab 界面设计

2026-09-15 · Codex（GPT-5）。新增测试功能的设计约束，非已实现 UI。

## 1. 使用场景与风格

用户在白天的笔记本工作台上编写和运行测试，需要同时理解请求、断言和失败结果。沿用现有 T2I 浅色、紧凑的产品界面，不切换成科研 Agent 的深色工作台，也不重绘现有 T2I 模块。

产品 register；克制色彩、信息优先。避免营销标题、大数字装饰卡片、重复卡片网格和默认弹窗。已有 PRODUCT.md 是基线，本文件补充测试界面规范。

现有壳层参考：68px 顶栏、224px 侧栏、系统中文字体、蓝色主操作。新组件使用低彩度冷中性色，OKLCH 可从背景 `oklch(0.975 0.008 255)`、面板 `oklch(0.992 0.004 255)`、正文 `oklch(0.25 0.02 255)` 起步，主色与现有蓝色视觉对齐；不借此全局修改 CSS。

正文 14px，辅助信息 12px，页面标题 22px；代码/日志 12px 等宽；行高正文 1.5。正文说明限制约 70ch。控件高 36px、列表行高 40px、圆角 8px，状态同时用图标与文字，不仅靠颜色。

## 2. 信息架构

侧栏新增分组「自动化测试」：测试工作台、用例目录、运行记录、BadCase、环境与工具。保留现有「提示词工作流」「裁判评测」「日志」。测试默认页为可操作的工作台，而非统计大屏。

主操作顺序：测试对象 → 测试类型 → 可用工具 → 测试内容 → 执行预览。工具不是所有类型都可任意选择，例如 pytest-asyncio 不用于 HTTP 压测，Postman 不用于源码单元测试；不兼容组合显示原因。

## 3. 工作台布局

```text
侧栏 224px │ 测试工作台                   环境：本地隔离 · Mock
           │ 对象 [T2I ▾]  类型 [接口 ▾]  工具 [pytest ▾]
           │ 搜索场景…  风险级别 [全部 ▾]           [预览执行]
           │ ───────────────────────────────────────────────
           │ □ 场景/目标接口      前置条件      上次结果
           │ □ 运行单参数校验     合成数据集    未运行
           │ □ 通道信息脱敏       无真实密钥    未运行
           │ ───────────────────────────────────────────────
           │ 已选 2 项 · 不调用模型 · 临时输出        [开始测试]
           │                          右侧可展开：请求/断言/工具操作
```

右侧检查面板约 340px，默认收起，选择行后打开；保留主表宽度。内容分「场景」「请求与断言」「工具操作」三个标签，不把教程散落主界面。

运行预览内联展示目标、用例数、执行器、费用模式、资源上限、输出目录及脱敏命令。确认按钮绑定预览 hash；修改配置使预览失效，避免用户确认后实际执行另一份任务。

## 4. 结果详情

顶部：状态、目标、批次、时长、通过/失败/错误/跳过计数、取消或重跑按钮。重跑创建新批次，不覆盖历史。

主区域：测试结果表；选中失败项查看「预期」「实际」「错误分类」「请求响应」「执行轨迹」「复现步骤」。日志默认折叠，支持搜索与复制脱敏片段。

Agent 页面加「行为检查」「质量评分」「来源证据」标签，展示四维理由而不是只有平均分。工具成功与任务成功分开。历史 BadCase 显示来源日期和复现状态；故障注入带明显标签，不伪装自然失败。

JMeter 页面按接口显示请求数、吞吐、分位时延、错误分布和原始 JTL 下载；只有一组数据时禁用“优化对比”。Allure/Jenkins 提供受控链接，远端报告不能直接无鉴权嵌入。缺失指标显示“未记录”，不显示 0。

## 5. 学习入口

「工具操作」面板为三步：如何准备 → 如何执行 → 如何判断结果。显示该用例对应的真实参数和断言，支持下载 Collection/JMX、复制复现命令。下载不执行；使用外部工具后可导入结构化结果并标识来源。

Postman 第一期支持导出、说明、Newman runner；不声称网页内已有完整 Postman。Jenkins 第一期显示已配置 job 与构建结果，不做可执行任意脚本的 Web IDE。

## 6. 状态、可访问性与小屏

- 草稿/准备中/等待批准/排队/运行/取消中/通过/失败/环境错误/已取消必须区分。
- 空目录显示“选择测试对象”或“导入用例”；未接工具显示“未配置”，不得使用 demo 假结果填充真实运行页。
- 预览、导出、浏览不调用付费 API。live 显示预算与确认，超限不自动追加。
- 桌面保留可收回的侧栏按钮；低于约 1100px 检查面板变为详情页，表格局部滚动；低于 768px 筛选纵向排列。新测试页不沿用全局 min-width 导致无法操作的布局，实施时局部限定修改范围。
- 键盘可选择用例、展开详情、切换标签；焦点明确，取消后焦点回原按钮；网络断开保留已有结果，重连按游标补拉。
- 动效仅用于状态变化，150–200ms，不采用装饰玻璃、弹跳或页面入场动画。

## 7. 实施检查

先接真实 catalog 和 preview，再接一个 pytest runner，最后接其他工具。不做一屏全部可点但都没有实现的按钮。需要占位时明确 disabled 原因。

真实浏览器验证：选择对象/类型/工具联动、预览不执行、运行取消、失败详情、断网恢复、窄屏侧栏、无敏感信息的导出。截图仅展示实际数据或明确标注的设计示意。
